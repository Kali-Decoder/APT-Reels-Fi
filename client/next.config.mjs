/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["hashpack.b-cdn.net", "ipfs.io", "s3.amazonaws.com","s3-us-west-2.amazonaws.com"],
      }
};

export default nextConfig;
